# Server environment. No secrets belong in client source.
# Leave unset for a read-only local preview.
# Enable ONLY behind the Sites dispatcher, which supplies trusted identity headers.
# Never enable this on a directly reachable Worker or another host.
# MANDATE_AUTH_MODE=sites-dispatch
